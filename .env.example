# Supabase — get publishable key via MCP: get_publishable_keys
NEXT_PUBLIC_SUPABASE_URL=https://aztmrbygerrqkragsncz.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your_publishable_key_here

# Site
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_BRAND_NAME=QuoreStack

# Server-only — from Supabase Dashboard > Settings > API (never expose to client)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Optional lead alerts (pick one or more — no Resend needed)
# Discord: Server settings → Integrations → Webhooks → New Webhook → copy URL
DISCORD_WEBHOOK_URL=

# Telegram: talk to @BotFather, then message your bot and get chat id via getUpdates
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# Or any custom webhook endpoint that accepts JSON POST
LEAD_WEBHOOK_URL=
